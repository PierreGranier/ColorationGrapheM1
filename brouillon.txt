// TODO
dans le rapport, on pourra faire un tableau comme le prof : colonne nom du fichier, colonne best K du prof et autant de colonnes best K que nous avons implémenté d'algos
le recap de coloriage doit etre envoyé dans un fichier texte

// en trouvant la clique maximale d'un graphe on peut trouver son nombre chromatique

// technique de coloriage :
// pour chaque sommet, chercher parmi ses voisins la plus petite couleur disponible (non utilisée) et l'attribuer directement au sommet
plus_petite_couleur = 0;
for (int j = 0; j < NOMBRE_SOMMETS; ++j) {
	if (MATRICE_ARETE[i][j] == '1' && couleur(j) == plus_petite_couleur) {
		++plus_petite_couleur;
		j = 0;
	}
}

for(int i = 0; i< NOMBRE_SOMMETS; ++i) {
	associer_couleur(i, chercher_premiere_couleur(i));
}

// autre technique, plus opti ? onsépa, fotester
// pour chaque sommet i, chercher parmi ses voisins la plus petite couleur disponible (non utilisée) pour i et l'attribuer directement au sommet i
// pour chaque sommet j, si il est voisin de i et qu'il n'a pas de couleur on lui attribut la plus petite couleur dispo pour j

// à mettre dans l'intro du compte rendu, en dessous de l'explication du sujet donnée par le prof :
n <= c <= d + 1
n : ordre du plus grand sous-graphe complet (clique)
c : nombre chromatique (nombre de couleurs permettant de colorier un graphe sans que deux sommets voisins soient de la même couleur)
d : degré le plus élévé des sommets


https://www.rocq.inria.fr/secret/Anne.Canteaut/COURS_C/chapitre3.html
https://openclassrooms.com/courses/apprenez-a-programmer-en-c/les-chaines-de-caracteres
